// Auth persistence is handled by the Auth.js adapter configuration.
// Keep this file as the module-local repository entry point for future auth-specific storage needs.
export const authRepository = {};
